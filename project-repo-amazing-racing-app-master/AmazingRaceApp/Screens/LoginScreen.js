import React from "react";
import {Image, ImageBackground, StyleSheet, View, Text, } from "react-native";

const LoginScreen=() => {
    return (
      <View>
        <Text>Hi guys this is the sign up screen!!!</Text>
      </View>
    );
  
    const styles = StyleSheet.create({});
  }

  
  export default LoginScreen;
  